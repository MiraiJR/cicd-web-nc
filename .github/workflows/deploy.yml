name: Deploy app

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: 18
      - run: |
          npm install
          cd frontend
          npm install
          npm run build
          nohup pm2 start npm --name "front-end" -- run dev &
          cd ..
          nohup pm2 start npm --name "back-end" -- run start &
          pm2 save
